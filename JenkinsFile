@Library("shared-library") _
pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                helloWorld(name:"shruthi" , dayofweek:"Thursday")
            }
        }
    }
}

